function Home() {
  return (
    <div>
      <h2>
        Default component rendered inside the ExampleRoute component when there
        is no subpath
      </h2>
    </div>
  );
}

export default Home;
